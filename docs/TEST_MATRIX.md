# Test Matrix

| Feature | Automated evidence | Reference evidence | Status |
| --- | --- | --- | --- |
| Typed/validated settings IPC | `tests/unit/settings-validation.test.ts` | Not applicable | Pass |
| SQLite setting persistence across restart | `tests/integration/database.test.ts` | Reference persistence files observed | Pass |
| Secure main window defaults | `tests/smoke/window-options.test.ts` | Not applicable | Pass |
| Electron launch and renderer readiness | `pnpm test:electron-smoke` | Phase 0 shell only | Pass |
| Native close confirmation | `pnpm test:close-smoke`: request, cancel, second request, remember, quit, clean exit | Exact reference quit-dialog copy/actions inventoried over scoped CDP | Pass for current desktop shell |
| Standalone auxiliary windows | `tests/unit/auxiliary-window-validation.test.ts`, measured window-options assertions, `pnpm test:auxiliary-smoke`, and Browser QA across eight routes plus 390x844; Electron smoke checks seven viewports, log interaction, and Monitor loading-block behavior | Clean-profile mappings/titles/loading states, seven exact viewport measurements including User at 700x500, direct About capture, exact log empty state, and exact-window Monitor overlay | Pass for confirmed Phase 1 skeletons; About dimensions and remaining states pending |
| Monitor blocking-loading parity | Contract test, real-Electron smoke asserts disabled refresh/status, Browser QA verifies overlay coverage and 702px no-overflow, same-size compare records 0.4806% | Exact 702x502 reference window remains under a full-body `Đang tải...` overlay that intercepts the underlying refresh button | Pass for clean-profile loading state; loaded/error/recovery states pending |
| Guide loading / Feedback empty parity | Typed clean-profile contracts, real-Electron smoke, Browser QA at 800x542 and 700x600, and same-size compares record 0.3068% / 0.0448% | Exact-window reference captures confirm a full-body Guide loader and a completely empty Feedback body | Pass for both clean-profile states; loaded/error states pending |
| Setup split / Payment unavailable QR | Typed clean-profile contracts, Electron smoke asserts 156px setup pane and Payment prompt/placeholder, Browser QA verifies exact viewports/no overflow, same-size compares record 0.2080% / 0.6978% | Exact-window Setup two-pane capture; Payment renderer capture plus observed `windowOpen` clone rejection | Pass for clean-profile visible states; Payment loaded/webview recovery remains pending |
| User loading/recovery/network error | Typed lifecycle contract, real-Electron smoke exercises `Quay lại` → error → `Làm mới` → loading → recovery, verifies no overflow/framework overlay, and same-size compares record 0.5365% / 0.5363% / 0.8575% | Fresh isolated `user.html` and `user_web?` targets; resolved-undefined bridge result; exact loading, recovery, and back-triggered error captures | Pass for safe clean-profile lifecycle; authenticated webview content remains pending |
| About to log workflow | Browser QA in browser-dev and `pnpm test:auxiliary-smoke` in real Electron | Reference About exposes `Nhật ký`; direct isolated click did not create a target because the true framework launch context remains unknown | Pass rebuild behavior / reference ownership pending |
| Screenshot comparison evidence | `tests/unit/screenshot-compare.test.mjs` verifies a one-pixel mismatch and strict dimension rejection; `pnpm capture:ui` produces true PNGs and supports route/viewport filters; five CLI comparisons write reports, diffs, and overlays | Same-size 1550x838 pairs for login, projects, templates, and editor, plus a 1920x1030 Settings pair | Harness/capture pass; mismatch baselines recorded, visual parity still partial |
| Vite HMR without installer | Browser QA observed live text update without reload | Not applicable | Pass |
| Login validation, navigation, and visual baseline | Browser QA: meaningful desktop/mobile DOM, unique invalid submit, visible validation, password toggle, guarded legal notice, clean desktop console, no framework overlay, and 390x844 scroll width equals viewport; Electron compare records 5.6526% | Reference login captured at 1550x838 and 390x844 | Pass for mock UI; improved by 26.8391 percentage points, fine typography/shape offsets pending |
| Registration route and validation | `tests/unit/registration-validation.test.ts`; Browser QA covers login link, four sequential errors, synthetic valid state, referral normalization, clean console, and 390x844 overflow | Registration screen and invalid client states captured in an isolated clean reference profile | Pass for Phase 1 mock; live registration untested |
| Projects, templates, settings, editor routes | Browser QA and DOM snapshots | Four reference states observed | Pass for current mock routes; inventory incomplete |
| Mobile visual shell | Browser QA at 390x844: auth forms remain reachable, login-to-registration navigation works, no horizontal overflow, and console is clean | Reference login/registration captured at 390x844; both clip the form outside a non-scrollable viewport | Pass with intentional accessibility deviation; authenticated reference mobile states pending |
| Editor panel navigation and empty state | In-app Browser: `Hình nền` → `Của tôi`, empty state visible | Exact reference labels/states captured over CDP | Pass for mock UI |
| Editor text formatting and remaining enabled controls | `tests/unit/studio-text-style.test.ts` plus `pnpm test:text-inspector-smoke` at 1240x669 and 390x844: edit content, toggle alignment/bold/italic, apply preset, switch built-in/personal sticker tabs, select voice, verify no overflow/framework overlay/app console errors, and capture both states | Exact-window text inspector controls and source-selected state captured over CDP | Pass for audited Phase 1 local behavior; persistence and source transforms remain pending |
| Editor layer transform and ordering | `tests/unit/studio-layer-transform.test.ts` plus isolated Electron smoke at 1240x669, 390x844, and reference-sized 1536x824: preserve layer identity while reordering, expose eight handles, drag, resize, rotate, keyboard nudge, and retain an overflow-safe shell | Three exact 1920x1030 image/text/avatar selected-source captures with Moveable-style geometry and four-button floating toolbar | Pass for functional local behavior; exact reference constraints and persisted scene schema remain pending |
| Livestream settings dialog | In-app Browser: dialog opens, defaults render, username edits, mock save closes dialog and shows confirmation; console clean | Reference dialog controls/defaults captured over CDP | Pass for Phase 1 mock behavior |
| TikTok Shop manager / product-pin readiness | In-app Browser at desktop and 390x844: open mock, waiting-login/empty state, retry action, guarded pin toggle remains false, actionable notice, internal mobile scrolling, and clean console | Exact-window `autopin.open()` audit confirms a dedicated reference-owned Chrome profile/CDP process, `checking` → waiting-login status, and unavailable/empty products while signed out | Pass for audited Phase 1 disconnected lifecycle; authenticated products, real single pin, and scheduler belong to Phase 11 |
| Editor responsive shell | In-app Browser at capture-equivalent 1240x670 verifies measured desktop boxes and intentional vertical overflow; 390x844 verifies page scroll width equals client width, internal tool-rail scrolling, tool switching, and background empty state | Desktop reference editor captured; mobile reference pending | Pass desktop mock / rebuild-only mobile |
| Provider settings form | In-app Browser at 1536x824 and 390x844: Grok add form opens, required validation renders, disposable local-only JSON validates, form clears, success notice appears, no horizontal overflow, console clean; Electron compare records 3.6747% after loading bundled Inter | Reference 1920x1030 screenshot plus labels/actions captured over CDP | Pass for safe Phase 1 mock; visual parity partial |
| Veo3 account table | In-app Browser: seeded account table renders and disable action changes `Rảnh` to `Đã tắt` | Reference columns/states/actions captured over CDP | Pass for mock UI |
| Persistent project lifecycle | `tests/unit/project-validation.test.ts`, `tests/integration/database.test.ts`, and `pnpm test:project-persistence-smoke`: schema v3 validation, v1/v2 scene/export upgrade, pre-migration `.bak`, interrupted `.tmp` recovery, create, rename, duplicate, touch/open, delete, debounced scene/settings autosave, download export, clean-profile file import, Electron restart, and editor restoration | Reference create/delete controls are visible, but mutation dialogs/menus cannot be safely activated without an authenticated reversible fixture | Pass local persistence, recovery, and portable JSON lifecycle / reference interaction parity pending |
| Per-project settings and media recovery | `tests/integration/media-files.test.ts` verifies present/missing detection; persistence smoke restores image controls, avatar product/script data, TikTok username, voice/cooldowns/trigger state, verifies one present and one missing path, exposes a repair action, and preserves settings through clean-profile export/import | Reference configured editor/live states and native picker behavior remain fixture-dependent | Pass rebuild behavior; exact reference interaction parity pending |
| Global settings persistence and recovery | `tests/unit/settings-validation.test.ts` rejects secret-shaped/invalid documents; `pnpm test:settings-persistence-smoke` verifies dedicated IPC rejection, safe Grok metadata, Veo enable state, active tab, Electron restart, invalid-database fallback, explicit reset, and absence of disposable JSON | Reference provider account persistence is visible, but real account/cookie behavior is intentionally not exercised | Pass secure rebuild behavior; live/reference provider verification pending |
| Last project startup restoration | `pnpm test:project-persistence-smoke` opens a project, restarts Electron, and verifies startup returns directly to the same validated project while deleted projects stay deleted | Reference startup restoration semantics remain unconfirmed without a reversible authenticated fixture | Pass plan exit criterion / reference behavior pending |
| TikTok Live normalized connector | `tests/unit/live-connector.test.ts` covers five interaction normalizers, incomplete gift suppression, connect/reconnect/disconnect/clear, stale listener rejection, stream-end, error cleanup, recording, replay, and an isolated room probe that always releases its adapter without changing an active session. `pnpm test:live-connector-smoke` exercises real Electron/preload/IPC/UI with exactly five mock events, counters `1,1,12,1,1`, recording download, reconnect, replay, stop, and route-leave cleanup. Browser QA verifies the former Phase 3 placeholder now calls the typed probe boundary and remains contained at 390x844 with a clean console | Reference offline/config dialog is inventoried, but a consensual real room and five-event set are not available | Pass for probe, mock, and lifecycle behavior; real-room five-event verification pending |
| Trigger/filter/moderation decisions | `tests/unit/moderation-engine.test.ts` classifies 55 Vietnamese fixtures and covers normalization, all required filters, allow/block precedence, output bans, duplicate/global/user boundaries, disabled/ignore actions, traces, and live settings replacement; project validation/persistence tests cover schema v4 migration and export/import; Electron live smoke changes chat to `ignore` during a connected mock session without reconnecting | Reference default trigger/cooldown values are inventoried; exact configured skip ordering, keyword controls, and feed decision display remain unavailable | Pass for Phase 4 rebuild criteria / exact reference parity pending |
| Product catalog and deterministic matcher | `tests/unit/product-catalog.test.ts`, `tests/unit/project-validation.test.ts`, full 58-test gate, and `pnpm test:project-persistence-smoke`: schema v5 migration, CRUD helpers, validation, versioned catalog JSON, accent-insensitive scoring, exact-name `1000`, threshold `160`, disabled exclusion, top-five candidates, autosave, restart, project export, and clean-profile import; Browser QA verifies editable desktop/mobile modal and visible matcher result | Only compatibility score/threshold defaults and signed-out TikTok Shop empty state are confirmed; authenticated catalog fields and exact reference scoring remain unavailable | Pass for Phase 5 rebuild criteria / `REF-014` parity pending |
| AI providers and fact-grounded reply engine | `tests/unit/ai-provider-service.test.ts`, `tests/unit/ai-reply-engine.test.ts`, `tests/unit/ai-reply-coordinator.test.ts`, schema-v6 project migration/persistence, `pnpm test:ai-reply-smoke`, and live connector smoke: mock/OpenAI-compatible/OpenRouter/Ollama contracts, public metadata persistence with secret exclusion, models, retry, timeout/cancel, prompt preview, 45-word/220-character cleanup, banned/hidden content, unsupported fact rejection, safe fallback, later-request recovery, feed output, clear cancellation, restart/export/import | Only compatibility length defaults are confirmed; exact reference provider/prompt/reply behavior and real account-backed output are unavailable | Pass for Phase 6 rebuild criteria / real providers and `REF-015` parity pending |
| TTS providers and non-overlapping interaction queue | `tests/unit/tts-provider-service.test.ts`, `tests/unit/interaction-queue.test.ts`, schema-v7 project migration/persistence, and `pnpm test:tts-queue-smoke`: session-secret exclusion, mock/HTTP/Windows contracts, timeout/cancel, cache/LRU clear, all queue states, 100 active cap, 300-job sequential run, skip/clear/retry, cache reuse, idle/talking state, live moderation-to-TTS path, restart/export/import; Browser QA verifies 390x844 containment, clean console, preview, and queue controls | Reference storage types confirm TTS/provider/voice fields and the 120-second compatibility timeout, but configured provider, playback, queue, cancellation, recovery, and avatar synchronization states are unavailable | Pass for Phase 7 rebuild criteria / live providers and `REF-016` parity pending |
| Phase 8 scene runtime and editor history | `tests/unit/scene-history.test.ts`, `tests/unit/project-validation.test.ts`, `tests/unit/studio-layer-transform.test.ts`, `tests/unit/chroma-key.test.ts`, database integration, project persistence, text-inspector, and TTS-queue smokes: schema-v9 migration, controlled MP4/GIF/avatar sources, nested snapshot isolation, bounded undo/redo for layer/text/image/avatar mutations, real Cancel drafts, snapping, pixel keying, GIF frame advancement, stable idle/talking node identity, and restart/export/import persistence. Earlier Browser QA proves desktop/mobile stacking, grid/zoom/thumbnails, clean console, and 390x844 containment | Selected-layer reference captures confirm selection handles, four order actions, and inspector structure; configured media, exact chroma behavior, history semantics, and persistence remain fixture-dependent | Pass for Phase 8 rebuild criteria / exact configured-media reference parity pending |
| Phase 9 local scene runtime | `tests/unit/scene-runtime-service.test.ts` and `pnpm test:scene-runtime-smoke`: loopback random port, health/ready/log, bounded bodies/logs, controlled built-in and project media IDs, traversal rejection, SSE snapshot/patch/reconnect, clean close, Browser Source URL UI, 115 ms latest propagation, stable GIF/avatar nodes, idle/talking patches, and a 1.53% same-scene visual diff | Reference metadata confirms the scene JSON -> local SSE -> HTML renderer -> OBS Browser Source pipeline, but a safe configured output URL/state is unavailable | Pass for Phase 9 rebuild criteria / `REF-017` configured reference parity pending |
| Phase 10 Studio desktop layout & OBS output | `tests/unit/manual-video-playback.test.ts`, `tests/unit/studio-source-cleanup.test.ts`, `tests/unit/obs-service.test.ts`, `tests/unit/scene-runtime-service.test.ts`, `pnpm test:obs-smoke`, and desktop Browser QA at 1280x720 and 1280x800 | Reference metadata confirms four-rail operator layout, scene JSON -> SSE -> OBS Browser Source pipeline; private native OBS package read-only | Pass for Phase 10 rebuild criteria (139 unit tests pass); real OBS virtual-camera hardware driver verification tracked in Phase 12 gaps |

| Phase 11 TikTok Shop and sequential pin scheduler | `tests/unit/shop-service.test.ts`, `tests/integration/shop-browser-smoke.test.ts`, `pnpm test:shop-ipc-smoke`, the 141-test gate, and in-app Browser QA: public config/runtime persistence, signed-out state, dedicated profile/random loopback CDP, Chrome/Edge detection, scrolling/deduplication, mapping, exact remote-ID pin despite similar names, controlled selector-failure screenshot, bounded retry, pause/resume/skip/wrap/stop, active state across renderer reload, safe idle restoration after Electron restart, browser-close recovery, process/profile cleanup, desktop/mobile containment, and clean console. `scripts/open-shop-live-verification.mjs` opens a persistent app-owned verification profile and the real seller dashboard without reading cookies; the current fixture reaches the seller registration/login surface | Reference confirms only the dedicated profile, port 9223, opening/waiting-login states, and signed-out empty result. Authenticated products, live selectors, pin side effects, and scheduler behavior still require manual login to the dedicated non-secret seller fixture | Pass for mock/local-browser foundation and real signed-out launch / authenticated live exit criteria and `REF-011` pending |
| Phase 12 diagnostics and resilience | Diagnostics/resilience/queue/scene unit tests, database integration tests, `pnpm test:diagnostics-smoke`, `pnpm test:recovery-smoke`, `pnpm test:multiservice-soak`, dedicated-browser Shop smoke, full 127-test gate, and in-app Browser QA: redaction; seven health providers; 2,200-event bound/restart; throttled/suppression-counted internal transitions; queue enum-only IPC without viewer text; six Electron cycles/three forced crashes/three stale-lock recoveries; database recovery/quarantine; native/app locks; exact-owned orphan cleanup; bounded redacted recovery notices; detail expansion; Diagnostics navigation; session dismissal; desktop and exact 390x844 containment with clean consoles | Reference clean log window exposes only `Mở tệp` and `Không có tệp nhật ký`; loaded logs, health aggregation, redaction, export/clear, visible recovery messaging, stale-lock/orphan cleanup, and interrupted recovery remain unavailable | Pass for current rebuild diagnostics/resilience, recovery-notice, and restart-soak slice / longer media-network soak and `REF-019` reference evidence pending |
| Phase 13 long-session simulation | `pnpm test:long-session-soak` defaults to 480 minutes and uses isolated Electron/profile/CDP resources. The latest 2-minute qualification covers 58 iterations/5 fault cycles while exercising mock AI, TTS, scene idle/talking, queue diagnostics, Shop exact-ID pinning, OBS camera cycles, TikTok/OBS/Shop recovery, seven health providers, bounded/redacted logs, scene-runtime availability, persisted progress, graceful shutdown, lock removal, and cleanup. CDP sampling records renderer 12.9 MB heap/305 nodes and scene runtime 1.9 MB heap/33 nodes under configurable growth bounds | No equivalent safe configured reference long-session fixture is available; real OBS and authenticated Shop remain blocked under `REF-018` and `REF-011` | Harness, bounded resource sampling, and 2-minute wall-clock qualification pass / required eight-hour run, real fixtures, and reference parity sign-off pending |
| Phase 13 restart during queued work/autosave | `pnpm test:active-work-restart`: isolated launch establishes a committed scene, starts a long mock TTS queue item, changes the canvas within the 350 ms autosave debounce, force-terminates the exact Electron tree, and restarts the same profile. Assertions cover valid committed scene restoration, marker persistence, stale-lock notice, seven health providers, zero queue jobs, idle avatar/queue state, clean console, graceful final shutdown, lock removal, and profile cleanup | Reference active-queue/autosave crash behavior is unavailable without a safe configured fixture | Pass for rebuild crash/restart invariant / exact reference behavior pending |
| Avatar-script AI generation | `tests/unit/avatar-script-generator.test.ts` has twelve tests for bounded fact-only prompts, untrusted-field flattening, 12,000-character IPC bounds, actual mock `AiProviderService` delegation, provider-output parsing, hidden-content/commercial-claim rejection, exact cancellation, stale late-response suppression, and provider/cancellation failure recovery. `pnpm test:avatar-script-smoke` verifies configured mock generation, delayed loopback-provider cancellation, Cancel isolation, Save/autosave, restart persistence, clean shutdown, and profile cleanup. The full gate passes 139 tests; in-app Browser QA passes at 1280x800 and 390x844 with no overlay, console warnings, or horizontal overflow | Reference dialog structure and disabled video action are captured; configured AI results, errors, cancellation, and video output remain unknown | Pass for rebuild AI-generation vertical slice / reference behavior and avatar-video output pending |
| Template application dialog | In-app Browser: first template opens preview dialog and `Sử dụng mẫu` produces confirmation notice | Reference template grid captured; application dialog not safely activated | Pass rebuild behavior / reference pending |
| Template visual baseline | Browser QA at 1240x669 and 390x844 confirms ten cards, clean console, preview/apply notice, no mobile horizontal overflow, and a contained dialog; fresh Electron capture plus screenshot compare records 45.3324% | Reference 1550x838 grid geometry/copy and artwork roles | Improved by 7.4855 percentage points; independent subject identity/poster illustration remain |
| Editor leave/save transition | In-app Browser: route leave exposes saving overlay, then completes navigation without console errors | Exact reference copy observed while leaving editor | Pass for Phase 1 visual state |
| Profile management dialog | In-app Browser: whitespace-only validation, valid save, session notice, clean console | Hidden reference profile dialog fields/actions inventoried without retaining private values | Pass for safe Phase 1 mock |
| Creator-first Home workflow | In-app Browser at 1280x800 and 390x844: clear page purpose and primary CTA, compact sidebar/support grouping, no visible DEV/local/audit labels, derived project status, `Tiếp tục` action, overflow menu, create dialog, project navigation, `scrollWidth === clientWidth`, no Vite overlay, and clean console | Preserves the observed dark shell and role-shaped campaign/product previews; intentionally departs from the earlier engineering-heavy Home geometry | Pass for focused usability redesign; exact reference screenshot parity is intentionally not claimed for this screen |
| Editor geometry | Desktop QA at 1240x669 logical capture viewport: titlebar x=5/y=5/h=40, project bar y=45/h=36, tools w=140, left stack x=145/w=300/h=574, canvas x=445/w=856/h=574, poster x=738.56/y=129/w=268.88/h=478; fresh Electron compare records 12.6332%; 390x844 capture remains vertically usable with internal tool/source scrolling | Matching computed reference measurements and 1550x838 screenshot anchors | Improved by 10.6777 percentage points; independent subject identity and remaining controls pending |
| Avatar dialogs | In-app Browser: library, add form, missing-video validation, clean console | Hidden reference avatar-library/add-dialog structure and guidance inventoried | Pass for Phase 1 mock |
| Full reference feature set | Phase 1-13 tests | Inventory pending | Pending |

## 2026-07-29 editor-source additions

| Feature | Automated/manual evidence | Reference evidence | Status |
| --- | --- | --- | --- |
| Typed source selection and inspectors | Electron QA selects image, text, and avatar rows independently; matching inspectors and functional selection geometry render; desktop/mobile consoles remain clean; internal scrolling stays contained | Exact-window selected-source captures and observed Moveable handles/order toolbar | Pass for the audited Phase 1 local behavior; exact reference persistence semantics belong to Phase 8 |
| Avatar-script dialog | Browser QA covers Manual/Manager tabs, adding products/scripts, guarded AI feedback, local save/close, disabled video generation, and 390x844 containment | Exact-window dialog inventory and screenshot with private account values excluded | Pass for the safe Phase 1 mock; real generation, persistence, and video output remain pending |
| Current editor visual baseline | Electron capture and strict same-size comparison record 12.6332% mismatch after dark-scrollbar alignment and independent perfume-scene role matching | 1550x838 editor screenshot plus selected-source/dialog captures | Partial visual parity; 10.6777 percentage points better than the original 23.3109% editor baseline |

## 2026-07-29 login-state additions

| Feature | Automated/manual evidence | Reference evidence | Status |
| --- | --- | --- | --- |
| Login input validation | `tests/unit/login-validation.test.ts` covers malformed email, missing password, and valid syntax | Clean-profile fields and application-handled submission | Pass: 4 focused tests |
| Password visibility and remember default | Desktop/mobile Browser QA confirms the password/text toggle and unchecked default | Exact-window reference eye-toggle and form-state probes | Pass for the audited Phase 1 mock |
| Invalid-credential recovery | `.invalid` fixture renders the exact observed error without external requests; Browser QA confirms the alert, unchanged route, and enabled submit; all 19 tests pass | Local rejection of the reference `user.apiPost` bridge produced the error after about 100 ms with the button still enabled | Pass for the safe local failure fixture |
| Login legal/support links | Browser QA confirms the guarded Terms notice; equivalent local handlers cover Policy and Help | Reference pointer links produced no route or bridge call under scoped tracing | Phase 1 guarded behavior; destinations remain unknown |
