# Reference Gaps

| ID | Area | Unknown behavior | Planned resolution | Status |
| --- | --- | --- | --- | --- |
| REF-001 | Full application | Complete route, dialog, state, and control inventory is not yet captured. | Continue the Phase 1 read-only audit from every top-level screen and project tool. | Open |
| REF-002 | Reference process | The reference can exit shortly after launch under some profile/launch combinations. | Use a dedicated audit profile for login states and the existing profile for authenticated states; record launch context with each capture. | Open |
| REF-003 | Settings security | Reference UI instructs users to paste exported Grok cookies. | Preserve visible parity where required but design real provider adapters around safer manual browser login/connection flows. | Open |
| REF-004 | Visual assets | Reference templates and branding cannot be copied. Existing independent host/product photos and CSS recomposition improve role parity, but the built-in ImageGen tool is unavailable in this session and the skill forbids silently switching to a key-backed CLI. | Continue with independent assets and use the approved built-in generator when available; compare layout/role rather than artwork identity. | Open |
| REF-005 | Desktop audit control | Windows foreground focus can fail, causing coordinate clicks to target another active application. | Do not use global coordinate clicks again; resume only with a window-scoped automation mechanism or explicit user-approved recovery. | Open |
| REF-006 | Export video | Clicking `Xuất video` blocks the CDP action and may open a native file/export surface that is not visible to the DOM audit. | Re-audit with a safe native-dialog-aware harness; do not confirm or save an export during Phase 1. | Open |
| REF-007 | Auxiliary pages | Seven standalone viewport sizes, About content, log empty, Guide loading, Feedback empty, Monitor loading, Setup split-empty, Payment unavailable-QR, and the User loading/recovery/network-error loop are captured/rebuilt. Payment opening still returns an inconsistent clone error. The About viewport, real owning controls, native log-file behavior, and authenticated webview states remain unknown. | Trace remaining launch controls and authenticated/loaded states only with safe fixtures; do not fabricate parity evidence. | Partial |
| REF-008 | Project/template mutations | A clean isolated profile works, but server-validated auth prevents it from reaching authenticated create, delete, rename, duplicate, or template-apply flows with synthetic credentials. The rebuild now persists functional create/rename/duplicate/open/delete workflows, but exact dialog/menu parity remains unverified. | Use a dedicated non-secret reference test account or another reversible service-backed fixture; keep the rebuild interaction surfaces replaceable. | Blocked on safe authenticated fixture |
| REF-009 | Reference mobile layout | Login and registration are captured at 390x844 and confirm a clipped, non-scrollable desktop composition. Authenticated routes, editor, and dialogs remain uncaptured at mobile size. | Continue exact-window CDP viewport emulation on safe routes and an authenticated fixture when available; do not reproduce inaccessible clipping in the rebuild. | Partial |
| REF-010 | Remaining overlays/dialogs | Quit, profile, avatar, and About are inventoried, but native pickers, configured live/audio states, source transforms, and recovery overlays remain incomplete. Exact-window CDP can inspect safe renderer states, but native/mutating paths still require a reversible fixture. | Resume selector/filechooser-scoped audit only for safe states; never copy tokens/profiles or confirm a native mutation. | Open |
| REF-011 | TikTok Shop product pinning | The signed-out lifecycle is confirmed: `autopin.open()` launches a dedicated application-owned Chrome profile, reports opening then waiting-login, and exposes no products. The rebuild has its own isolated Chrome/Edge CDP adapter, exact-ID single pin, mapping, persisted sequential scheduler, diagnostics, and safe loopback fixture evidence. A persistent rebuild-owned real verification profile now opens the seller dashboard and reaches the registration/login surface without reading cookies. Authenticated product schema, live selectors, pin effects, stop/recovery, and scheduler behavior remain unverified. | Complete manual login in the dedicated non-secret seller fixture, then compare visible product identity, exact pin side effect, selector recovery, timing, and browser closure without inspecting or reusing personal profiles/cookies. | Partial; real signed-out launch complete, authenticated verification waiting on manual login |
| REF-012 | TikTok Live connected feed | The rebuild has deterministic mock coverage for chat, gift, like, follow, share, stream-end, errors, reconnect, recording, and replay. The former UI placeholder now performs an isolated real connector probe with timeout/cleanup and does not disturb an active session. The reference configured feed and an appropriate real room producing all five interaction types are not safely available. | Use a consensual public/test room and a non-secret reference fixture. Record only normalized interaction evidence; never retain cookies, account profiles, or private viewer data. | Partial; real room-probe complete, event verification blocked on safe room/reference fixture |
| REF-013 | Moderation decision semantics | Reference defaults confirm enabled events and cooldown values, but filter ordering, duplicate identity scope, allow/block precedence, banned-output behavior, and any visible decision trace are not observable in the offline state. | Compare with a safe configured reference feed. Keep the pure engine reason codes and ordering replaceable until exact behavior can be observed. | Partial; rebuild engine complete |
| REF-014 | Product catalog and matcher semantics | Compatibility defaults confirm exact-name score `1000` and threshold `160`, but the authenticated reference catalog fields, validation, import/export format, token weights, tie-breaking, disabled-product handling, and any matcher diagnostics are not safely observable. | Compare against a dedicated non-secret configured project/product fixture. Keep catalog adapters and scoring constants replaceable; never inspect personal seller data. | Partial; rebuild catalog/matcher complete |
| REF-015 | AI provider and reply semantics | The 45-word target and 220-character ceiling are compatibility defaults, but exact reference provider choices, prompt assembly, model discovery, retry/cancellation behavior, fallbacks, output cleanup, and product-fact enforcement are not observable in the offline/reference-safe state. | Use non-secret test provider accounts/endpoints and a configured reference project. Compare prompts only through visible preview/output surfaces; never retain keys, hidden prompts, cookies, or personal account data. | Partial; rebuild AI engine complete |
| REF-016 | TTS providers and interaction queue semantics | Reference storage types expose server/model/voice/reference-audio fields and the 120-second compatibility timeout, but the safe offline state does not reveal configured provider requests, voice discovery, cache behavior, queue ordering, skip/clear/retry semantics, failure recovery, audio overlap, or idle/talking synchronization. | Use a dedicated non-secret configured reference project plus safe local HTTP/Windows provider fixtures. Record visible state/timing only, never keys, voice samples, account profiles, or private audio. Keep provider and playback adapters replaceable. | Partial; rebuild TTS/queue complete |
| REF-017 | Local scene runtime and Browser Source output | Reference metadata confirms a scene JSON -> local SSE -> HTML renderer -> OBS Browser Source pipeline, but the safe offline project does not expose the bound endpoint, payload/patch format, readiness/log behavior, reconnect timing, configured media output, or OBS-facing browser frame. | Use a dedicated non-secret configured reference project and isolated OBS/browser fixture. Record endpoint behavior and visible frames only; never retain account data, private media, cookies, or proprietary runtime code. | Partial; rebuild scene runtime complete |
| REF-018 | OBS scene, Browser Source, and virtual camera | The reference private native OBS package cannot be reused, and the safe reference state does not expose scene/source ownership, resolution/FPS behavior, current-scene restoration, virtual-camera errors, or restart recovery. A checksum-verified official OBS 32.2.1 portable fixture now proves authenticated WebSocket connection, dedicated Browser Source create/update, reconnect, scene switch/restore requests, and a ready SSE scene client. OBS reports `VirtualCam is not available` because no virtual-camera driver is installed; the app now treats this as a capability state instead of disconnecting. | With explicit approval for a system driver change, install the official OBS virtual-camera driver and verify a camera consumer. Continue exact reference comparison through a disposable configured project without inspecting personal scenes. | Partial; real OBS/Browser Source complete, virtual-camera driver/consumer and reference parity pending |
| REF-019 | Logs, health diagnostics, and resilience | The reference clean log window exposes only `Mở tệp` and `Không có tệp nhật ký`; no safe loaded-log fixture exposes health aggregation, redaction, export/clear, crash recovery, stale locks, or orphan cleanup. The rebuild now has typed redacted diagnostics, seven health providers, throttled internal transitions, validated SQLite recovery, native/app instance locks, exact-owned Shop-browser orphan cleanup, and a six-cycle/three-crash Electron soak. | Use a disposable configured reference profile and controlled failures that contain no secrets. Compare visible log levels/search/export/clear/health/recovery behavior, then run a longer wall-clock media/network soak in both apps without reading personal logs. | Partial; rebuild restart-soak slice complete, reference and extended-duration evidence pending |

## 2026-07-29 gap update

- `REF-010` is now partial rather than wholly unknown: source-specific text, image, and avatar inspectors; selection handles; and the avatar-script dialog are inventoried.
- The rebuild text inspector now implements every audited local control: content, font, size, color, alignment, bold, italic, and twenty deterministic presets update the visible scene. Sticker tabs and voice selection also have explicit local behavior, so no enabled editor button found by the current static inventory remains handler-less.
- The rebuild now implements the visible selected-layer contract from the three exact captures: eight resize handles, center/origin and rotation controls, keyboard/pointer movement, resize/rotate updates, plus top/up/down/bottom layer ordering. The exact reference drag math and persistence semantics remain unverified because an isolated clean profile cannot reach the authenticated editor.
- The rebuild now uses a narrow native picker for adding/replacing typed project media references and visibly reports broken paths after restart/import. Exact reference picker copy, filters, cancellation behavior, and path-repair presentation remain unverified.
- Remaining `REF-010` work is reference-backed transform mutation/persistence semantics, reference native-picker parity, configured live/audio states, avatar-script AI result/error/cancellation semantics, avatar-video output, and recovery overlays. The rebuild AI-generation vertical slice is implemented and verified but does not close the missing reference evidence.
- The clean Monitor full-body loading overlay is now confirmed and rebuilt. It blocks the underlying refresh control; the previous simulated refresh transition was removed because it was not reference-backed.
- Guide is now confirmed as a full-body loading surface and Feedback as a completely empty light body. Neither clean-profile state exposes an actionable body control.
- Setup is confirmed as an empty 156px/remaining-width split layout. Payment is confirmed as an unavailable blank QR surface with scan guidance; the rebuild intentionally does not reproduce the reference bridge serialization defect.

## 2026-07-29 login-state update

- Login password visibility, remember-default, and invalid-credential recovery are now confirmed through an isolated bridge-failure probe.
- Successful authentication, server-specific error variants, retry timing under a real network outage, and session restoration still require a dedicated non-secret account and belong to later authentication/persistence work.

## 2026-07-29 Phase 12 recovery-notice update

- `REF-019` remains partial. The rebuild now exposes bounded, redacted startup notices for database recovery/quarantine, stale or invalid runtime locks, exact-owned Shop-browser orphan termination, and owner-mismatch refusal.
- Desktop and exact 390x844 QA confirm plain-language copy, technical-detail expansion, Diagnostics navigation, per-tab-session dismissal, clean consoles, and no horizontal overflow.
- No safe reference fixture currently exposes loaded logs, health aggregation, crash/stale-lock/orphan recovery, or equivalent visible recovery messaging. No parity claim is made.
- The next evidence target is a longer wall-clock media/network soak plus a disposable configured reference diagnostics/recovery inventory that does not read personal logs or credentials.
