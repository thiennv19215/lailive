# Product Specification

The source of truth is `NEW_REPO_FULL_PLAN.md` plus read-only observation of LivestreamAgent 1.4.0. The application must provide full usable parity across projects, TikTok events, moderation, products, AI, TTS, avatar scenes, browser output, OBS, TikTok Shop, diagnostics, and recovery.

The implementation must remain independently authored and use public dependencies. Product completion requires both automated tests and screen-by-screen reference evidence.
